@Library('jenkins') _
pipeline {
    agent any

    stages {
        stage('Example') {
            steps {
                script {
                    jenkins-shared-library.groovy
                }
            }
        }
    }
}
