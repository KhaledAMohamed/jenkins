@Library('jenkins') _
pipeline {
    agent any

    stages {
        stage('Example') {
            steps {
                script {
                    HelloWorld.groovy
                }
            }
        }
    }
}
